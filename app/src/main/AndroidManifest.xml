<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-feature
        android:name="android.software.car.templates_host"
        android:required="true" />

    <uses-permission android:name="android.car.permission.TEMPLATE_RENDERER" />

    <application
        android:label="Caramel Vanilla Templates Host"
        android:directBootAware="true"
        android:hasCode="true">
        <service
            android:name=".TemplatesHostService"
            android:exported="true">
            <intent-filter>
                <action android:name="android.car.template.host.RendererService" />
            </intent-filter>
        </service>
    </application>
</manifest>
